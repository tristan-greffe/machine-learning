import{_ as s,a as t,b as a,c as i}from"./chunks/dataset_piscines.CSxrKepq.js";import{_ as o,o as n,c as l,a0 as r}from"./chunks/framework.Ckbw5VQO.js";const y=JSON.parse('{"title":"Dataset","description":"","frontmatter":{},"headers":[],"relativePath":"guide/models/dataset.md","filePath":"guide/models/dataset.md"}'),d={name:"guide/models/dataset.md"};function p(c,e,h,g,m,u){return n(),l("div",null,[...e[0]||(e[0]=[r(`<h1 id="dataset" tabindex="-1">Dataset <a class="header-anchor" href="#dataset" aria-label="Permalink to &quot;Dataset&quot;">​</a></h1><p>In <strong>geo-ml</strong>, there is <strong>no manual annotation</strong>. That is the core principle of the project: labels already exist as free, open vector data covering all of France. Building polygons come from <strong>BD TOPO IGN</strong>, pool polygons from <strong>OpenStreetMap</strong>.</p><p>The pipeline&#39;s role is purely mechanical: download IGN satellite tiles, assemble mosaics, convert GPS polygons into pixel bounding boxes, then slice everything into 640×640 windows in the format expected by YOLO.</p><div class="tip custom-block"><p class="custom-block-title">No manual annotation</p><p>Labels cover all of France. We download, convert, train - without ever drawing a box by hand.</p></div><h2 id="label-sources" tabindex="-1">Label sources <a class="header-anchor" href="#label-sources" aria-label="Permalink to &quot;Label sources&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Object</th><th>Source</th><th>Layer / tag</th></tr></thead><tbody><tr><td><strong>Buildings</strong></td><td><a href="https://geoservices.ign.fr/bdtopo" target="_blank" rel="noreferrer">BD TOPO IGN</a></td><td><code>BDTOPO_V3:batiment</code></td></tr><tr><td><strong>Pools</strong></td><td><a href="https://www.openstreetmap.org/" target="_blank" rel="noreferrer">OpenStreetMap</a></td><td><code>leisure=swimming_pool</code></td></tr></tbody></table><p>Both sources return <strong>GPS polygons</strong> (GeoJSON) that the pipeline converts into pixel bounding boxes.</p><h2 id="yolo-format" tabindex="-1">YOLO format <a class="header-anchor" href="#yolo-format" aria-label="Permalink to &quot;YOLO format&quot;">​</a></h2><p>YOLOv8 expects a dataset structured around a <code>dataset.yaml</code> configuration file that describes paths and class names. See the <a href="https://docs.ultralytics.com/datasets/detect#supported-dataset-formats" target="_blank" rel="noreferrer">official Ultralytics format</a>.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>model/dataset/&lt;model&gt;/</span></span>
<span class="line"><span>├── dataset.yaml          ← paths + class names</span></span>
<span class="line"><span>├── images/</span></span>
<span class="line"><span>│   ├── train/  *.jpg     ← 640×640 windows (80 %)</span></span>
<span class="line"><span>│   └── val/    *.jpg     ← 640×640 windows (20 %)</span></span>
<span class="line"><span>└── labels/</span></span>
<span class="line"><span>    ├── train/  *.txt     ← one line per object</span></span>
<span class="line"><span>    └── val/    *.txt</span></span></code></pre></div><div class="tip custom-block"><p class="custom-block-title"><code>train/</code> vs <code>val/</code></p><p>The dataset is split <strong>80 % / 20 %</strong> at write time:</p><ul><li><strong><code>train/</code></strong>: images the model sees during training. It computes its errors on them and updates its weights at each pass.</li><li><strong><code>val/</code></strong>: images the model <strong>never sees</strong> during training. They are used solely to measure real quality at the end of each epoch. If performance on <code>val</code> degrades while <code>train</code> keeps improving, this signals <strong>overfitting</strong>.</li></ul></div><h3 id="label-file-structure" tabindex="-1">Label file structure <a class="header-anchor" href="#label-file-structure" aria-label="Permalink to &quot;Label file structure&quot;">​</a></h3><p>Each image <code>images/train/000042.jpg</code> has an associated file <code>labels/train/000042.txt</code>. Each line in the <code>.txt</code> describes <strong>one object</strong>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>0  0.666  0.655  0.156  0.181</span></span>
<span class="line"><span>↑    ↑      ↑      ↑      ↑</span></span>
<span class="line"><span>class  cx    cy     w      h    (all normalised to [0.0, 1.0])</span></span></code></pre></div><div class="tip custom-block"><p class="custom-block-title">Field details</p><ul><li><strong><code>class</code></strong>: class identifier (<code>0</code> = building or pool depending on the model). A single integer.</li><li><strong><code>cx</code>, <code>cy</code></strong>: coordinates of the <strong>centre</strong> of the box, normalised between <code>0.0</code> and <code>1.0</code> (<code>0</code> = left/top edge, <code>1</code> = right/bottom edge).</li><li><strong><code>w</code>, <code>h</code></strong>: <strong>width</strong> and <strong>height</strong> of the box, normalised the same way. All values are relative to the 640×640 image - no absolute pixels.</li></ul></div><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># dataset.yaml (auto-generated)</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">path</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/abs/path/to/model/dataset/buildings</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">train</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">images/train</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">val</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:   </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">images/val</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">names</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">building</span></span></code></pre></div><h2 id="pipeline-overview" tabindex="-1">Pipeline overview <a class="header-anchor" href="#pipeline-overview" aria-label="Permalink to &quot;Pipeline overview&quot;">​</a></h2><p>The pipeline starts from two distinct data sources - one vector (annotations), one raster (imagery) - that converge to produce the final dataset.</p><img src="`+s+'" style="display:block;margin:1.5rem auto;width:100%;height:auto;"><p><strong>1. Fetching annotations (vector source)</strong></p><ul><li>Download GPS polygons from open data: <strong>BD TOPO</strong> (via WFS) and <strong>OpenStreetMap</strong> (via Overpass).</li><li>These polygons delimit the objects to detect (pools, buildings).</li></ul><p><strong>2. Fetching imagery (raster source)</strong></p><ul><li>Download <strong>256×256 px</strong> tiles from <strong>IGN WMTS</strong> (<code>data.geopf.fr</code>) at zoom 19 (~20 cm/px).</li><li>Disk-cached in <code>model/dataset/_tilecache/</code> to avoid re-downloading the same tiles.</li></ul><p><strong>3. Assembling the mosaic</strong></p><ul><li>Stitch <strong>4×4 tiles</strong> into a <strong>1024×1024 px mosaic</strong> (~200 m × 200 m on the ground) at native resolution.</li></ul><p><strong>4. Coordinate conversion</strong></p><ul><li>GPS polygons are projected onto the mosaic: <strong>GPS → pixel</strong> conversion, producing <strong>bounding boxes</strong> expressed in pixels.</li></ul><p><strong>5. Window slicing</strong></p><ul><li>The mosaic is sliced into <strong>640×640 px windows</strong> (YOLO&#39;s input size), using a slicing strategy specific to each model.</li></ul><p><strong>6. Final dataset generation</strong></p><ul><li>Each window produces an image + annotation pair, split between training and validation: <ul><li><code>images/train/</code> and <code>images/val/</code> → <code>*.jpg</code> images</li><li><code>labels/train/</code> and <code>labels/val/</code> → <code>*.txt</code> annotations (YOLO format)</li></ul></li></ul><h3 id="why-assemble-a-mosaic" tabindex="-1">Why assemble a mosaic? <a class="header-anchor" href="#why-assemble-a-mosaic" aria-label="Permalink to &quot;Why assemble a mosaic?&quot;">​</a></h3><p>A raw IGN tile is 256×256 px - too small for a 640×640 YOLO window. Upscaling blurs the image and defeats the purpose of using high-resolution IGN imagery. By assembling 4×4 tiles, we get a 1024×1024 px mosaic at native resolution from which we can cut multiple 640×640 windows. It is also exactly the same scale as at inference time, ensuring the model sees the same resolution during training and in production.</p><img src="'+t+'" style="display:block;margin:1.5rem auto;width:100%;height:auto;"><h2 id="two-strategies-by-density" tabindex="-1">Two strategies by density <a class="header-anchor" href="#two-strategies-by-density" aria-label="Permalink to &quot;Two strategies by density&quot;">​</a></h2><p>Buildings and pools are distributed very differently. The pipeline adapts its slicing strategy to each object type.</p><h3 id="buildings-sliding-window-on-a-grid" tabindex="-1">Buildings - sliding window on a grid <a class="header-anchor" href="#buildings-sliding-window-on-a-grid" aria-label="Permalink to &quot;Buildings - sliding window on a grid&quot;">​</a></h3><p>Buildings are <strong>dense and uniformly distributed</strong>. For each zone, we assemble a large mosaic then <strong>slide a 640×640 window with 25% overlap</strong> across the entire surface, ensuring exhaustive coverage without cutting objects at the edges.</p><img src="'+a+'" style="display:block;margin:1.5rem auto;width:100%;height:auto;"><p><strong>Steps per zone:</strong></p><ol><li>Download IGN tiles → assemble the mosaic.</li><li>Query <strong>BD TOPO WFS</strong> for building polygons in the zone.</li><li>Convert GPS polygons → pixel bboxes on the mosaic.</li><li>Slide the 640×640 window → write image + YOLO labels.</li></ol><h3 id="pools-object-centred-window-with-jitter" tabindex="-1">Pools - object-centred window with jitter <a class="header-anchor" href="#pools-object-centred-window-with-jitter" aria-label="Permalink to &quot;Pools - object-centred window with jitter&quot;">​</a></h3><p>Pools are <strong>rare and scattered</strong>: grid slicing would mostly produce empty images. Instead, for <strong>each pool</strong>, we download a mosaic centred on it, then cut a window with a random offset (<strong>jitter ±120 px</strong>) to improve position invariance.</p><img src="'+i+`" style="display:block;margin:1.5rem auto;width:100%;height:auto;"><p><strong>Steps per zone:</strong></p><ol><li>Query <strong>OSM Overpass</strong> → <code>leisure=swimming_pool</code> polygons.</li><li>Filter out out-of-range pools (<code>&lt; 1.5 m</code> or <code>&gt; 35 m</code>).</li><li>For each pool: download a centred mosaic.</li><li>Cut a 640×640 window with jitter → label <strong>all</strong> visible pools → write.</li></ol><div class="warning custom-block"><p class="custom-block-title">Label every pool in the window</p><p>If a window contains 3 pools but only one is marked, we teach the model that the other 2 are background → <strong>false negatives</strong>. The script therefore labels <strong>all</strong> pools present in the window, not just the one used for centring.</p></div><h2 id="download-the-dataset" tabindex="-1">Download the dataset <a class="header-anchor" href="#download-the-dataset" aria-label="Permalink to &quot;Download the dataset&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">conda</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> activate</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> geoml</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -m</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> model.dataset.main</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">              # buildings + pools (default)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -m</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> model.dataset.main</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --buildings</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # buildings only</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -m</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> model.dataset.main</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --pools</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      # pools only</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Argument</th><th>Description</th></tr></thead><tbody><tr><td><em>(none)</em></td><td>Generates both datasets (buildings + pools)</td></tr><tr><td><code>--buildings</code></td><td>Generates only the buildings dataset (BD TOPO WFS)</td></tr><tr><td><code>--pools</code></td><td>Generates only the pools dataset (OSM Overpass)</td></tr></tbody></table><div class="info custom-block"><p class="custom-block-title">Dataset imprecision</p><p>BD TOPO and OSM polygons are not always <strong>perfectly aligned</strong> with the IGN orthophoto - a 1–2 m offset is common. Bounding boxes may therefore be slightly shifted from the real object. YOLO tolerates this label noise well, but it is the main source of dataset imprecision.</p></div>`,51)])])}const w=o(d,[["render",p]]);export{y as __pageData,w as default};
